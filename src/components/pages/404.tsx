import * as React from 'react'

import styled from 'styled-components'
import { m, s, Row, c, Column, Box } from 'styles/index'

const NotFound = (props: Object) => {
  return (
    <Box center flex1 h='100%'>
      Not Found
    </Box>
  )
}

export default NotFound