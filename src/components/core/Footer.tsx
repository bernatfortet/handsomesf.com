import * as React from 'react'

import styled from 'styled-components'
import { Box, Column, m, Row, s } from 'styles'

type Props = {
}

const Footer = (props: Props) => {
  const { } = props

  return (
    <Box w={'100%'} center mt={40} op={0.4}>
      Handsome Goods Co. © 2018
    </Box>
  )
}

export default Footer