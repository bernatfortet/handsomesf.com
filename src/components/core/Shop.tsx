import * as React from 'react'

import styled from 'styled-components'
import { Box, c, Column, m, Row, s } from 'styles'

type Props = {
}

const Shop: React.SFC<Props> = (props: Props) => {
  const {} = props

  return(
    <Box>
      Shop
    </Box>
  )
}

export default Shop